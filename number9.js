// == : Checks only the values for equality and performs type coercion if 
// the values are different types
// E.g
//  5 = '5'; true, because '5' is coerced into a number before comparison

//  While
// === : Checks both the values and the types for 
// equality without performing type coercion.
// E.g
// 5==='5'; false , because one is a number and the other is a string.
